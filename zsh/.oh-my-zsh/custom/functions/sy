local tmp cwd
tmp="$(mktemp -t "yazi-cwd.XXXXXX")" || return

command sudo HOME="$HOME" yazi "$@" --cwd-file="$tmp"

cwd="$(<"$tmp")"
if [ -n "$cwd" ] && [ "$cwd" != "$PWD" ] && [ -d "$cwd" ]; then
    builtin cd -- "$cwd"
fi

rm -f -- "$tmp"
